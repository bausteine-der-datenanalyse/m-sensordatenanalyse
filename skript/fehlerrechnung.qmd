# Fehlerrechnung

``` {python}
#| echo: false

# Module laden
import numpy as np
import numpy.polynomial.polynomial as poly
import pandas as pd
import matplotlib.pyplot as plt
import scipy
import glob
```

::: {.border layout="[[5, 90, 5], [1]]"}

&nbsp;

"Das Experimentieren ist ein dornenvoller Weg und so ganz sicher ist man sich nie." 

&nbsp;

Wagner, Paul. 2015. "Einführung in die Physik I. PH I - 04 - Die Messgenauigkeit / Fehlerrechnung Teil 1." Universität Wien Physik, [YouTube](https://youtu.be/sF2_1YNiWXI?feature=shared&t=245), Zeitstempel 04:05
:::

::: {.border}
**to do:**

- Eden-Gebhard-2014 auf -2024 aktualisieren oder entfernen
- Hinweis: statistische Überlegung gelten für große N und Normalverteilung der Werte
- Fehlerfortpflanzung gilt für unkorrellierte Größen (korrelierte Größen sind komplizierter und nicht Teil dieses Kapitels)
- Angabe Messunsicherheit mit Standardabweichung nach DIN

**Aufbau kann vereinfacht werden.**

1. einmalige Messung einer Größe

2. mehrmalige Messung einer Größe

  a) zufälliger Anteil

  b) systematischer Anteil

3. einmalige Messung mehrerer Größen (lineare Fehlerfortpflanzung = Größtfehler)

4. mehrmalige Messung mehrerer Größen (Gauß'sche Fehlerfortpflanzung = Standardmessunsicherheit)

Erst Fehlerarten erklären, dann DIN einführen, weil die so kompliziert ist.
:::

&nbsp;

Kein Messgerät misst exakt und Messdaten streuen zufällig um ihren wahren Wert. Das Ziel der Fehlerrechnung ist es, die sich daraus ergebende gesamte Messunsicherheit zu quantifizieren.

## Bevor es losgeht
Das Thema ist jedoch mit einigen Schwierigkeiten verbunden, die den Einstieg und die eigenständige Vertiefung des Themas erschweren.

### Der Begriff des Fehlers
Das Wort Fehler steckt zwar schon in der Überschrift dieses Kapitels, doch handelt es sich schon seit 1995 nicht mehr um einen genormten Begriff. Die DIN 1319 normt Grundbegriffe und Verfahren der Messtechnik. Die Norm ersetzt den Begriff des Fehlers durch die Begriffe **Messabweichung** und **Messunsicherheit**, umfasst aber darüber hinaus eine Vielzahl aufeinander bezogener Begriffsdefinitionen, Anmerkungen und Bemerkungen.  
Die DIN 1319 bzw. ihre Teile sind zuletzt zwischen 1995 und 2005 aktualisiert worden und in der über die Bibliothekssysteme bereitgestellten Form nicht sonderlich gut zu lesen und nicht vollständig maschinell durchsuchbar.

In diesem Kapitel werden die in der Fehlerrechnung gebräuchlichen Begriffe aufgegriffen, um die Verbindung zu den genormten Begriffen herzustellen. Im Allgemeinen werden jedoch die genormten Begriffe verwendet.

### Konfidenzniveau
Wiederholte Messungen enthalten eine zufällige Komponente statistischer Unsicherheit. Die statistische Unsicherheit kann immer nur mit einer Vertrauenswahrscheinlichkeit angegeben werden. Die Berechnung von Konfidenzintervallen haben wir im Kapitel @sec-hooke behandelt.

Zur statistischen Analyse gehört als erster Schritt, das Konfidenzniveau, das gelten soll, festzulegen. Hieraus leiten sich die zu verwendenden Vielfachen der Standardabweichung $s$ bzw. des Korrekturfaktors $t$ ab. Hierbei gibt es je nach Disziplin unterschiedliche Konventionen.

:::{layout="[5, 90, 5]"}

&nbsp;

"In der Physik und der Vermessungstechnik begnügt man sich mit einer statistischen Sicherheit von 68,3% und setzt deshalb t = 1 [...] (In der Industrie wird t = 2, in der Biologie t = 3 bevorzugt.)" [@HS-Aalen-2016 S. 4]

&nbsp;
:::

In der ingenieurswissenschaftlichen Literatur wird dieser Schritt nicht immer explizit gemacht (weil es üblich ist, $1 s$ bzw. $1 t$ zu verwenden). Dagegen heißt es in der DIN 1319: "Es ist üblich, das Vertrauensniveau ($1 - \alpha$) in Prozent anzugeben, wobei meist $\alpha = 0,05$ (Vertrauensniveau: 95 %) benutzt wird." [@DIN1319-1, S. 17]

### Notation
Die Notation ist komplex: neben dem lateinischen und griechischen Alphabet wird auch das kyrillische Alphabet verwendet. Die Notation wird außerdem uneinheitlich gehandhabt. Während in der DIN 1319 Messabweichungen mit $e$ notiert werden, sind in der Literatur $\epsilon$ (epsilon) und $\Delta$ (Delta) gebräuchlich, wobei letzteres auch für den Größtfehler verwendet wird.

Übrigens: Das in der DIN verwendete $e$ steht für error, also Fehler ( ¯\\\_(ツ)\_/¯ ).

## Fehlerarten
Bei Messungen können unterschiedliche Arten von Fehlern auftreten. Am gebräuchlichsten ist die Unterteilung in grobe, systematische und zufällige *Fehler*. Nach DIN 1319 sollte besser von systematischen und zufälligen Messabweichungen gesprochen werden. Grobe Fehler werden in der Norm jedoch nicht behandelt und hier ist der Fehlerbegriff durchaus angemessen. 

  - Systematische Messabweichungen: unter identischen Bedingungen konstante, d. h. nach Betrag und Vorzeichen gleiche, Verzerrung der Messergebnisse durch Abweichungen der Messgeräte. Beispielsweise:
  
    - in Abhängigkeit von der Temperatur
    - fehlerhafte Kalibrierung
    - Alterung der Bauteile
    - die Art der Messung, z. B. Ablese- und Skalenfehler, Messintervall bei digitalen Messgeräten ([Quantifizierungsfehler](https://de.wikipedia.org/wiki/Quantisierungsabweichung))

  - Zufällige Messabweichungen: statistische Streuung der Messwerte um ihren Erwartungswert. Beispielsweise:
    
    - Stichprobenfehler
    - Ablesefehler
  
  - Grobe Fehler: Verfälschen der Messergebnisse. Beispielsweise durch:
  
    - falschen Versuchsaufbau
    - ungeeignete Messgeräte
    - falsches Ablesen, z. B. Missachtung der [Einschwingzeit](https://de.wikipedia.org/wiki/Einschwingzeit)) oder Unachtsamkeit (z. B. Mitwiegen des Gefäßes)

[@Hempel-2016, S. 1-2; @EdenGebhard-2024, S. 17-18]

Bestimmte Tätigkeiten, wie das Ablesen, können mehreren Fehlerarten zugehörig sein:

  - Unaufmerksames Ablesen und Flüchtigkeitsfehler sind grobe Fehler,
  - winkel- und positionsabhängige Fehler wie der [Parallaxenfehler](https://de.wikipedia.org/wiki/Parallaxenfehler) sind systematische Abweichungen,
  - Fehler infolge des begrenzten Auflösungsvermögens des menschlichen Auges führen zu zufälligen Abweichungen.

::: {#tip-umgangmitfehlern .callout-tip}
## Umgang mit verschiedenen Arten von Messabweichungen

1. Grobe Fehler: betroffene Werte streichen und Messung wiederholen.

2. Systematische Messabweichungen: Systematische Messabweichungen werden, wenn sie bekannt sind, korrigiert (bspw. temperaturabhängige Genauigkeit des Messgeräts) oder quantifiziert (bspw. Genauigkeit des Ablesens).

3. Zufällige Messabweichungen: Die statistische Streuung von Messwerten um den Erwartungswert wird quantifiziert.

Einige systematische und zufällige Messabweichungen können nur mit hohem Aufwand reduziert werden (genauere Messgeräte, größere Anzahl von Messvorgängen). Der Aufwand muss gegen den Gewinn an Genauigkeit abgewogen werden: Ist das Messergebnis mit der angegebenen Unsicherheit akzeptabel?

(@Hempel-2016, S. 2)

:::

## Absolute und relative Fehler
In der Fehlerrechnung werden absolute und relative Fehlerangaben unterschieden. Hier sollte besser von absoluter und relativer Messunsicherheit gesprochen werden.

  - Die **absolute Messunsicherheit** $u$ wird in der Einheit der gemessenen Größe angegebenen: $u(m) = 0,2 kg$. Das vollständige Messergebnis lautet: $m = 5,0 \pm 0,2 kg$ oder  $m = 5,0 kg \pm 0,2 kg$.
  
  - Die **relative Messunsicherheit** $u_{rel}(m) = \frac{u(x)}{ | \bar{x}_E | }$ wird entweder als Produkt in der Form  $m \cdot (1 \pm \frac{u(x)}{\bar{x}_E})$ angegeben. Bspw.:

      $m = 5,0 kg \cdot (1 \pm \frac{0,2 kg}{5,0 kg}) = 5,0 kg \cdot (1 \pm 0,04)$
  
    Alternativ wird die relative Messunsicherheit in Prozent $u_{rel}(m) = \frac{u(x)}{\bar{x}_E} \cdot 100 ~ \%$ angegeben: $m = 5,0 kg \pm 4 ~ \%$.


[@DIN1319-1, S. 9-10, 16; @DIN1319-3, S. 10]

::: {#wrn-notation .callout-warning}
## (Alternative) Notationen

In der DIN 1319 werden Messabweichungen mit $e$, die Messunsicherheit mit $u$ notiert. In der Literatur zur Fehlerrechnung wird zumeist der Begriff des (Mess-)Fehlers verwendet. Die Notation ist allerdings uneinheitlich.

  - Der absolute Fehler wird häufig mit $\epsilon x$ (epsilon x) notiert. Üblich ist aber auch die Notation mit $\Delta x$ (Delta) (beispielsweise [@HS-Aalen-2016; @Dinter-2011]). $\Delta$ wird aber ebenfalls für den Größtfehler verwendet. 

  - Der relative Fehler wird auch mit $\delta x$ (delta x) notiert, bspw: $\delta x = 4 \%$.

:::

## Standardfehler vs. Größtfehler
ergänzen: Standardfehler... in der Regel quadratische Kombination... ein Prinzip, das uns in der Fehlerfortpflanzung wieder begegnen wird.

Systematische Messabweichungen $e(x)_{s}$ bewirken eine einseitige Verzerrung des Messergebnisses, sodass der Messwert immer zu hoch oder zu niedrig ausfällt. Zufällige Messabweichungen $e(x)_{r}$ führen unkontrolliert mal zum Über-, mal zum Unterschätzen des Messwerts.

Im Allgemeinen werden sich verschiedene Messabweichungen ausgleichen. Man spricht in diesem Fall vom **Standardfehler** bzw. besser von der Standardmessunsicherheit, auf die wir in diesem Kapitel noch genauer eingehen werden. An dieser Stelle soll zunächst das Grundprinzip vermittelt werden, die quadratische Kombination der beteiligten Messabweichungen.

$$
u(x) = \sqrt{ {e(x)_{s}}^2 + {e(x)_{r}}^2 }
$$

Diese Form der Berechnung ist in der DIN 1319 niedergelegt.

Als Größtfehler wird der ungünstigste Fall bezeichnet, bei dem sich alle Messabweichungen jeweils um ihren maximalen (oder minimalen) Wert gegenseitig verstärken, sodass der Messwert die maximal mögliche Abweichung vom (unbekannten) tatsächlichen Wert aufweist.

Der **Größtfehler** wird als $\Delta x$ (Delta x) notiert und ermittelt, indem man die Beträge der einzelnen Messabweichungen addiert.

$$
\Delta x = | e(x)_{s} | + | e(x)_{r} |
$$

Da Messungen bei groben Fehlern als ungültig anzusehen sind, entfällt der Anteil grober Fehler $| e(x)_{grob} |$.

(@EdenGebhard-2024, S. 35-36)

Werden mehrere Größen betrachtet, so werden die Messabweichungen der beteiligten Größen zur Messunsicherheit kombiniert bzw. addieren sich die Größtfehler der beteiligten Größen zum Größtfehler des Messergebnisses. Die Berechnung wird in den folgenden Abschnitten behandelt. 

::: {#tip-welcherfehler .callout-tip}
## Standardfehler vs. Größtfehler
Der Größtfehler ist eine vereinfachte Form der Fehlerabschätzung und überschätzt die wahrscheinliche Messunsicherheit. Die Vereinfachung besteht darin, dass für den Größtfehler keine erweiterte Messunsicherheit oder kein Vertrauensintervall berechnet werden muss.
:::

## DIN 1319
Die DIN 1319 normt Grundbegriffe und Verfahren der Messtechnik.

  - Teil 1 DIN 1319-1 und Teil 2 DIN 1319-2 definieren Grundbegriffe der Messtechnik und für Messmittel.
  - Teil 3 DIN 1319-3 befasst sich mit der Auswertung von Messungen einer *einzelnen* Messgröße und deren Messunsicherheit. DIN 1319-3 wird auch angewendet, wenn die Meßgröße mittels einer gegebenen Funktion aus anderen Größen berechnet wird (beispielsweise die Geschwindigkeit $v$ als Quotient der Strecke $s$ und der Zeit $t$).
  - Teil 4 DIN 1319-4 befasst sich mit der Angabe von Messergebnissen und deren Messunsicherheit, wenn die Messgrößen aus anderen Größen berechnet werden, z. B. durch eine Ausgleichsrechung.

Das Lernziel dieses Kapitels besteht darin, dass Sie die folgende Grafik erklären können.

::: {.border}
![Messergebnis nach DIN 1319](00-bilder/fehlerarten-nach-DIN-1319.png)

[@DIN1319-1, S. 30]
:::

&nbsp;

### Messabweichung
Der in der Fehlerrechnung gebräuchliche Begriff des (Mess-)Fehlers meint im engeren Sinn die Messabweichung nach DIN 1319, also die *tatsächliche Abweichung* eines einzelnen Messwerts oder des arithmetischen Mittelwerts einer Messreihe (dem unberichtigten Messergebnis) vom wahren Wert.

::: {#imp-messabweichung .callout-important}
## Messabweichung nach DIN 1319

Die Messabweichung ist die: "Abweichung eines aus Messungen gewonnenen und der Meßgröße [...] zugeordneten Wertes vom wahren Wert [...]. Ist $m$ der der Meßgröße zugeordnete Wert und $x_w$ der wahre Wert, so ist die Meßabweichung des zugeordneten Wertes $m - x_w$ [...]." [@DIN1319-1, S. 12]
:::

Die Messabweichung des **unberichtigten Messergebnisses** setzt sich additiv aus der zufälligen Messabweichung $e_r$ und der systematischen Messabweichung $e_s$ zusammen.

  - Die systematische Messabweichung ist die Differenz aus Erwartungswert $\mu$ und wahrem Wert $x_w$. Es gilt: $e_s = \mu - x_w$
  - Die systematische Messabweichung $e_s$ setzt sich aus einem bekannten $e_{s,b}$ und einem unbekannten Anteil $e_{s,u}$ zusammen.
 - Die bekannte, systematische Messabweichung $e_{s,b}$ wird mit entgegengesetzten Vorzeichen $- e_{s,b}$ *Korrektion* genannt.

Das **Messergebnis** $\bar{x}_E$ ist das um die bekannte, systematische Messabweichung $e_{s,b}$ berichtigte arithmetische Mittel einer Messreihe $\bar{x}$, das als Schätzer des Erwartungswerts $\mu$ verwendet wird. $\bar{x}_E = \bar{x} - e_{s,b}$

[@DIN1319-1, S. 11-13]

### Messunsicherheit
Der wahre Wert einer gemessenen Größe ist in der Regel unbekannt, sodass die tatsächliche Messabweichung ebenfalls nicht bekannt ist. Deshalb kann nur eine *aus den Messgrößen gewonnene Schätzung* der Messabweichung - die Messunsicherheit - angegeben werden. Der in der Fehlerrechnung gebräuchliche Begriff des (Mess-)Fehlers meint im weiteren Sinn die Messunsicherheit nach DIN 1319.

Die Messunsicherheit einer Messgröße $Y$ besteht einerseits aus der zufälligen Messabweichung $e_r$ und andererseits aus der systematischen Messabweichung $e_s$.

**zufällige Messabweichung**

Wird eine Messgröße $Y = y(x)$ durch mehrere, direkte Messungen bestimmt, so streuen die Messwerte $x_i$ zufällig um ihren Erwartungswert $\mu$ mit der Standardabweichung $\sigma$. Der Erwartungswert $\mu$ und die Standardabweichung $\sigma$ sind in der Regel nicht bekannt und werden aus den Messwerten durch den arithmetischen Mittelwert $\bar x$ und die empirische Standardabweichung $s_{n-1}$ geschätzt. (siehe @sec-einleitung)

Als Unsicherheit der zufälligen Streuung wird die empirische Standardabweichung des Mittelwerts verwendet. 

$$
u(\bar x) = \frac{s_{n-1}}{\sqrt{N}} ~ = ~ \sqrt{\frac{1}{N(N-1)} \sum_{i=1}^{N}(x_i - \bar{x})^2}
$$

[@DIN1319-3, Gleichung (4), S. 5, Notation angepasst]

**systematische Messabweichung**

Die systematische Messabweichung $e_s$ setzt sich aus einem bekannten Anteil $e_{s,b}$ und einem unbekannten Anteil $e_{s,u}$ zusammen. Da der unbekannte Anteil nicht bestimmt werden kann, wird die gesamte systematische Messabweichung über die bekannte systematische Messabweichung $e_{s,b}$ geschätzt. Daraus ergibt sich eine Unsicherheit über die tatsächliche gesamte systematische Messabweichung $u(e_{s})$.

In der DIN 1319 werden zwei Fälle unterschieden: Die Unsicherheit $u(e_{s})$ wird als vernachlässigbar eingeschätzt. Sollte die Unsicherheit der systematischen Messabweichung nicht als vernachlässigbar angesehen werden, verweist die DIN 1319-3 auf Berechnungsregeln in Abschnitt 6.2.5 - auf die wir hier nicht eingehen. (@DIN1319-3, S. 5-6)

Das **berichtigte Messergebnis** $y$ berechnet sich durch die Korrektion der bekannten systematischen Messabweichung:

$$
y = \bar x - e_{s,b}
$$

[@DIN1319-3, Gleichung (6), S. 6, Notation angepasst]

Die Messunsicherheit des berichtigten Messergebnisses berechnet sich aus der quadratischen Kombination der zufälligen und der systematischen Messabweichung (die im Einzelfasll auch Null oder vernachlässigbar sein kann.).

$$
u(y) = \sqrt{u^2(\bar x) + u^2(e_s)}
$$

[@DIN1319-3, Gleichung (8), S. 6, Notation angepasst]

Mit der Messunsicherheit kann das **vollständige Messergebnis** angegeben werden, z. B. in der Form:

$$
Y = y \pm u(y)
$$

[@DIN1319-3, Gleichung (9), S. 6]

*Zusätzlich* zum vollständigen Messergebnis kann ein Vertrauensbereich (auch: Vertrauensintervall oder Konfidenzintervall) angegeben werden. Der Berechnung von Konfidenzintervallen haben wir im Kapitel @sec-normalverteilung vorgegriffen.

$$
y - t \cdot u(y) \le Y \le y + t \cdot u(y)
$$

[@DIN1319-3, Gleichung (14), S. 7]

Die Wahl eines Vertrauensbereichs ist Ermessenssache. Die DIN 1319 formuliert dazu: "Es ist üblich, das Vertrauensniveau ($1 - \alpha$) in Prozent anzugeben, wobei meist $\alpha = 0,05$ (Vertrauensniveau: 95 %) benutzt wird." [@DIN1319-1, S. 17] "Dieses Vertrauensniveau von 95 % sollte verwendet werden, aber kein höheres." [@DIN1319-3, S. 7] 

So ergibt sich beispielsweise:

$$
y - t_{\alpha / 2} \cdot \frac{s_{n-1}}{\sqrt{n}} \le Y \le y + t_{\alpha / 2} \cdot \frac{s_{n-1}}{\sqrt{n}}
$$

:::{#wrn-notation1 .callout-warning appearance="simple"}
## Notation in der DIN 1319
In der DIN wird der arithmetische Mittelwert einer Messreihe statt mit $\bar{x}$ mit $\bar{v}$ notiert. Die zufällige Messabweichung wird statt $u^2(\bar x)$ mit $u^2(x_1)$ und die systemtatische Messabweichung statt mit $u^2(e_s)$ mit $u^2(x_2)$ notiert.
:::

### Übersicht
Im folgenden Kasten finden Sie die wichtigsten Begriffsdefinitionen nach DIN 1319.

::: {#imp-DIN1319 .callout-important}
## Grundbegriffe nach DIN 1319

**Messabweichung**: "Abweichung eines aus Messungen gewonnenen und der Meßgröße [...] zugeordneten Wertes vom wahren Wert [...]. Ist $m$ der der Meßgröße zugeordnete Wert und $x_w$ der wahre Wert, so ist die Meßabweichung des zugeordneten Wertes $m - x_w$ [...]." [@DIN1319-1, S. 12]

  - Die Messabweichung des **unberichtigten Messergebnisses** setzt sich additiv aus der zufälligen Messabweichung $e_r$ und der systematischen Messabweichung $e_s$ zusammen.
 - Die systematische Messabweichung $e_s$ setzt sich aus einem bekannten $e_{s,b}$ und einem unbekannten Anteil $e_{s,u}$ zusammen.
 - Die systematische Messabweichung ist die Differenz aus Erwartungswert $\mu$ und wahrem Wert $x_w$. $e_s = \mu - x_w$
 - Die bekannte, systematische Messabweichung $e_{s,b}$ wird mit entgegengesetzten Vorzeichen $- e_{s,b}$ *Korrektion* genannt.

Das **Messergebnis** $\bar{x}_E$ ist das um die bekannte, systematische Messabweichung $e_{s,b}$ berichtigte arithmetische Mittel einer Messreihe $\bar{x}$, das als Schätzer des Erwartungswerts $\mu$ verwendet wird.  
$\bar{x}_E = \bar{x} - e_{s,b}$

[@DIN1319-1, S. 11-13]

**Messunsicherheit** $u$ oder $u(y)$: "Kennwert, der aus Messungen [...] gewonnen wird und zusammen mit dem Meßergebnis [...] zur Kennzeichnung eines Wertebereichs für den wahren Wert der Meßgröße [...] dient." [@DIN1319-1, S. 14]  

  - **Standardmessunsicherheit** oder kurz Standardunsicherheit wird verwendet, wenn die Messunsicherheit durch eine Standardabweichung ausgedrückt wird. [@DIN1319-3, S. 3]

  - **erweiterte Messunsicherheit** $U(y) = k \cdot u(y)$: Wertebereich, der den wahren Wert der Messgröße wahrscheinlich enthält. Der Erweiterungsfaktor $k$ sollte zwischen 2 und 3 festgelegt werden, vorzugsweise wird $k = 2$ verwendet. Der Erweiterungsfaktor $k$ ist mitzuteilen, damit die Standardmessunsicherheit $u(y) = U(y) / k$ ermittelt werden kann. [@DIN1319-3, S. 7] 

Das **vollständige Messergebnis** ist das Messergebnis mit quantitativen Angaben zur Messunsicherheit $u$. $x = \bar{x}_E \pm u$. [@DIN1319-1, S. 16]

  - Der **Vertrauensbereich** $\bar x - t \cdot u(y) \le Y \le y + t \cdot u(y)$ kann zusätzlich zum vollständigen Messergebnis angegeben werden. 
  
    - Ist die systematische Messabweichung bekannt und korrigiert worden, dann liegt der wahre Wert der Messgröße $Y$ innerhalb des Vertrauensbereichs.
    - Wenn die systematische Messabweichung nicht bekannt ist, liegt der Erwartungswert $\mu$ der Verteilung im Vertrauensbereich. [@DIN1319-3, S. 7]  

    *Hinweis: In der DIN wird der arithmetische Mittelwert einer Messreihe statt mit $\bar{x}$ mit $\bar{v}$ geschrieben*.
:::


## Signifikante Stellen
Bei der Angabe des vollständigen Messergebnisses ist die Anzahl der signifikanten oder zuverlässig bekannten Stellen zu beachten.

::: {#imp-signif .callout-important}
## signifikante Stellen

"Jede zuverlässig bekannte Stelle mit Ausnahme der Nullen, die die Position des Dezimalkommas angeben, wird signifikante Stelle genannt." [@EdenGebhard-2024, S. 24]

"Gerundete numerische Unsicherheitwerte sind **mit zwei (oder bei Bedarf mit drei) signifikanten Ziffern** anzugeben. **Sie sind aufzurunden**. Das Meßergebnis ist an derselben Stelle wie die zugehörige Unsicherheit zu runden, z.B. y = 245,5716 mm auf y = 245,57 mm, wenn u(y) = 0,4528 mm auf u(y) = 0,46 mm aufgerundet wird." [@DIN1319-3, S. 6, eigene Hervorhebung]
:::

Beispielsweise liegen die im vorherigen Kapitel verwendeten Abstandsmessung im Format $153,29$ vor, die Messwerte weisen also 5 signifikante Stellen auf. Für den arithmetischen Mittelwert einer Messreihe $153,008889$ gelten deshalb ebenfalls 5 signifikante Stellen. Es wird an der ersten nicht mehr signifikanten Stelle gerundet, sodass der arithmetische Mittelwert $153,01$ angegeben wird.

Die Anzahl signifikanter Stellen ist allerdings nicht immer zuverlässig zu bestimmen. Es gilt:

1. Alle Ziffern ungleich Null sind immer signifikant.
2. Führende Nullen sind niemals signifikante Stellen. Beispielsweise hat 0012 km 2 signifikante Stellen. 0,001 mm hat eine signifikante Stelle.
3. Nullen am Ende einer Zahl sind signifikant, wenn sie sich rechts vom Dezimaltrennzeichen befinden, bspw: 12,100 km hat 5 signifikante Stellen.
4. Nullen, die sich am Ende einer Zahl ohne Komma befinden, können signifikant sein. 1200 m kann zwei oder vier signifikante Stellen haben.

::: {.callout-tip}
## signifikante Stellen und wissenschaftliches Runden

Angaben zur Messunsicherheit werden zwar aufgerundet. Trotzdem an dieser Stelle: Kennen Sie den Unterschied zwischen kaufmännischem und wissenschaftlichem Runden?

{{< video https://www.youtube.com/watch?v=s0gPyypiGOs >}}

Runden und signifikante Stellen (Vorkurs Mathematik) von Edmund Weitz ist abrufbar auf [YouTube](https://www.youtube.com/watch?v=q2iZDtotiM0). 2019

Python rundet wissenschaftlich:

``` {python}
print(round(2.5), round(3.5))
```

:::


## Methoden zur Fehlerrechnung
Das Ziel der Fehlerrechnung ist es, Unsicherheitsintervalle für die gemessene Größe zu ermitteln. Dafür stehen verschiedene Methode zur Verfügung:

1. Fehlerabschätzung
2. Fehlerstatistik
3. Fehlerfortpflanzung

**Bei der Auswahl der Methode besteht ein Ermessensspielraum. (@Hempel-2016, S. 2) Ergänzen: Erwägungsgründe.**

### 1. Fehlerabschätzung
Die Fehlerabschätzung wird verwendet, wenn

  - **eine Größe**
  - **nur einmal**

gemessen wird. 

Da der wahre Wert eine Messgröße unbekannt ist, bezweckt die Fehlerabschätzung, die maximal mögliche Abweichung des Messwerts vom tatsächlichen Wert, also den **Größtfehler**, zu bestimmen. Bei einer einmaligen Messung gibt es keine statistische Komponente. In die Fehlerabschätzung eines einzelnen Messwerts gehen deshalb ein:

  - die Garantiefehlergrenze des Messgerätes (das vom Hersteller auf dem Gerät oder in der Bedienungsanleitung angegeben ist - siehe Beispiel), also die bekannte systematische Messabweichung $e_{s,b}$, und 
  - ein abgeschätzter Anteil, der sich nach den konkreten Bedingungen des Experiments, etwa der Skalenteilung des Messgerätes oder den Ablesebedingungen richtet. Dieser Anteil kann also aus einem systematischen Anteil (bspw. eine analoge Skala konnte nur aus einem Winkel abgelesen werden) und einem zufälligen Anteil (bspw. Ablesegenauigkeit des Messgeräts) bestehen.

Der Größtfehler $\Delta x$ ergibt sich aus der Addition der Beträge der bekannten systematischen Messabweichung und der geschätzten zufälligen Messabweichung: $\Delta x = |e(x)_{s,b}| + |e(x)_{r}|$.

(@Hempel-2016, S. 3)

#### Aufgabe Temperaturmessung
Mit einem digitalen Thermometer und einem thermoelektrischen Element vom Typ K wurde die Temperatur $T = 112,1 ~ °C$ gemessen. Welche systematischen und zufälligen Messabweichungen sind bekannt und können angegeben werden (siehe Beispiel)?

::: {#nte-thermometer .callout-note collapse="true" layout-ncol=2}
## Garantiefehlergrenze Thermometer
![Thermometer](/skript/00-bilder/thermometer.jpg)

![Spezifikationen](/skript/00-bilder/thermometerspecifications.jpg)
:::

::: {#tip-skalengenauigkeit .callout-tip}
## Halber oder ganzer Skalenstrich?

Messgeräte mit digitaler Anzeige machen es leicht: Die Ablesegenauigkeit beträgt eine Skaleneinheit. Bei Messgeräten mit analoger Anzeige ist es Ermessenssache, welche Messgenauigkeit Sie angeben können bzw. möchten. 

Sind Sie sich absolut sicher, dass Sie zwischen "genau auf dem Strich" und "zwischen zwei Strichen" unterscheiden können, beträgt Ihr Ablesefehler einen halben Skalenstrich. Sind Sie sich nicht sicher, beträgt Ihr Ablesefehler einen ganzen Skalenstrich.

(Siehe: [Fehlerrechnung leicht gemacht, FSU Jena](https://www.physik.uni-jena.de/pafmedia/9072/fehlerrechnungleichtgemachtpdf.pdf))

:::

::: {#tip-thermometer .callout-tip collapse="true"}
## Musterlösung Temperaturmessung

**systematische Messabweichung**

Die absolute Messabweichung beträgt $u(T) = 0,3 ~ °C$.
Die relative Messabweichung beträgt $0,05 ~ \%$. Das bedeutet:

$\frac{u(T)}{112,1 °C} \cdot 100 ~ \% = 0,05 ~ \%$

$\frac{u(T)}{112,1 °C} \cdot 100 = 0,05$

$u(T) = 0,05 \cdot \frac{112,1 °C}{100}$

$u(T) = 0,05605 °C$

Die bekannte systematische Messabweichung beträgt also:

$e(T)_{s,b} = 0,3 ~ °C+ 0,05605 ~ °C = 0,35605 ~ °C$

**zufällige Messabweichung**

Die zufällige Messabweichung beträgt eine Skaleneinheit, also:

$e(T)_{r} = 0,1 ~ °C$

**Größtfehler**

Der Größtfehler beträgt:

$\Delta T = |e(T)_{s,b}| + |e(T)_{r}|$

Die Messunsicherheit ist auf zwei Stellen aufzurunden:

$\Delta T = |0,35605 °C| + |0,1 ~ °C| = 0,45605 ~ °C \approx 0,46 °C$

Das vollständige Messergebnis lautet also $T = 112,1 \pm 0,46 ~ °C$.
:::

#### Beispiel Messbedingungen
Was ist mit den konkreten Bedingungen eines Messvorgangs gemeint? Das soll mit zwei einfachen Messvorgängen demonstriert werden.

**Hier können Sie mitmachen!**

Sie benötigen:

  - ein geeignetes Messinstrument, z. B. ein Lineal, und
  - eine Münze (idealerweise 1-Euro-Münze).

Im ersten Messvorgang soll der Durchmesser der 1-Euro-Münze bestimmt werden. 

::: {#nte-münze .callout-note collapse="true"}
## Erster Messvorgang Münze

Die Messung wird mit einem [Messschieber mit Nonius](https://de.wikipedia.org/wiki/Nonius) 0,05 mm nach DIN 862 durchgeführt. (DIN 862 wurde 2011 durch DIN EN ISO 13385-1 ersetzt, in der der Begriff der Abweichung verwendet wird. Die DIN 862 ist aber auf dem Messschieber aufgedruckt.) Die DIN 862 normiert symmetrische Fehlergrenzen von Messschiebern. Die Fehlergrenze ist abhängig von der Skalengenauigkeit des Messschiebers sowie von der gemessenen Länge und ist aus einer Tabelle abzulesen. 

Messwert: $x = 23,25 mm$

  - Die Skaleneinteilung ist 0,05 mm, die zufällige Ableseabweichung $e_r$ beträgt also $\pm 0,025 mm$.
  - Die DIN 862 normiert Fehlergrenzen, die abhängig von der gemessenen Länge zunehmen, mindestens aber 50 $\mu m$ oder 0,05 mm betragen. Für den Messwert 23,25 mm gilt somit als bekannte systematische Messabweichung $e_{s,b} = \pm 0,05 mm$.

Aus der Summe der Beträge ergibt sich aufgerundet der Größtfehler: $\Delta x = |0,05| mm + |0,025| mm = 0,075 mm$.

Das vollständige Messergebnis, bestehend aus dem Messwert und der Messunsicherheit lautet also: $23,25 ± 0,075 mm$. Der relative Größtfehler beträgt: $23,25 mm ± 0,32\%$.

:::

Im zweiten Messvorgang soll die Länge des Daumens bestimmt werden. ([Der Daumen besteht anatomisch aus nur 2 Fingergliedknochen](https://de.wikipedia.org/wiki/Daumen))

::: {#nte-daumen .callout-note collapse="true"}
## Zweiter Messvorgang Daumen

Die Messung wird erneut mit dem Messschieber durchgeführt.

Messwert: $x = 64,7 mm$

  - Die Skaleneinteilung ist 0,05 mm, die zufällige Ableseabweichung beträgt also $\pm 0,025 mm$.
  - Die von der DIN 862 für gemessene Längen zwischen 50 und 300 mm normierte Fehlergrenze beträgt 50 $\mu m$ oder 0,05 mm. Für den Messwert 64,7 mm gilt somit die Fehlergrenze $\pm  0,05 mm$.
  - zusätzlich wird eine symmetrische Messabweichung von 3 mm veranschlagt, da
    - das Messgerät nicht gerade angesetzt,
    - die Daumenwurzel nicht exakt lokalisiert und 
    - die elastische Fingerkuppe nicht fixiert werden konnte.

Aus der Summe der Beträge ergibt sich der Größtfehler $\Delta x = |0,025| mm + |0,05| mm + |3| mm = 3,075 mm \approx 3,1 mm$.

Das Messergebnis, bestehend aus dem Messwert und dem Fehlerintervall lautet also: $64,7 ± 3,1 mm$. Der relative Größtfehler beträgt: $64,7 mm ± 4,79 \%$.
:::

### 2. Fehlerstatistik
Die Fehlerstatistik wird verwendet, wenn 

  - **eine Größe**
  - **mehrfach**
  
gemessen wird.

Die Fehlerstatistik ist nur dann als zuverlässig anzusehen, wenn die Messabweichung zufällig ist, die Messwerte also betragsmäßig und mit wechselndem Vorzeichen streuen. Dann ist die Messabweichung in der ermittelten Standardabweichung enthalten. Liegt jedoch eine bekannte, systematische Messabweichung vor, muss das Messergebnis entsprechend korrigiert werden.

Der Fehlerstatistik haben wir in vorausgehenden Kapiteln mit der statistischen Beschreibung der empirischen Streuung des Mittelwerts vorgegriffen. Zur Wiederholung:

1. Legen Sie ein Signifikanzniveau $\alpha$ bzw. ein zweiseitiges Konfidenzniveau $1 - \frac{\alpha}{2}$ fest.
2. Bestimmen Sie den Stichprobenmittelwert $\bar{x}$.
3. Bestimmen Sie die Stichprobenstandardabweichung $s$, die Stichprobengröße $N$ und den empirischen Standardfehler $\frac{s_{n-1}}{\sqrt{N}}$.
4. Bestimmen Sie die Werte der Normal- bzw. der t-Verteilung für das gewählte Konfidenzniveau - für einen zweiseitigen Hypothesentest $\frac{\alpha}{2}$ und $1 - \frac{\alpha}{2}$
5. Berechnen Sie das Konfidenzintervall $\bar{x} \pm t_{\alpha / 2} ~ \frac{s_{n-1}}{\sqrt{n}}$.

Dieses Konfidenzintervall sagt für eine gewählte Wahrscheinlichkeit aus, in welchem Bereich der Mittelwert einer erneut durchgeführten Messreihe oder, wenn die systematische Messabweichung Null (oder vernachlässigbar) ist, der *wahre Wert* liegen wird.   
Dagegen gibt die Standardabweichung $s$ Auskunft darüber, in welchem Bereich ein einzelner Messwert liegen wird. Die Sicherheit dieser Aussage kann durch Ausweitung des Konfidenzintervalls erhöht werden (bspw. $2 \cdot s$). Auch hier ist der t-Wert als Korrekturfaktor für kleine Stichproben zu verwenden.

[@Hempel-2016, S. 3-7]

**Deshalb gab es noch die Messunsicherheit bei Hempel - die ist aber, wenn man die DIN 1319 als Maßstab nimmt, falsch beschrieben, außer es soll der Größtfehler berechnet werden.**

Ist der systematische Anteil der Messunsicherheit bekannt, sind das Messergebnis und die Messunsicherheit entsprechend zu korrigieren. Das **berichtigte Messergebnis** $y$ berechnet sich durch die Korrektion der bekannten systematischen Messabweichung:

$$
y = \bar x - e_{s,b}
$$

Die Messunsicherheit des berichtigten Messergebnisses berechnet sich aus der quadratischen Kombination der zufälligen und der systematischen Messabweichung.

$$
u(y) = \sqrt{u^2(\bar x) + u^2(e_s)}
$$

Hier Beispiel und pandas.sem() einführen

Datensatz hooke, Messreihe Abstandsmessung

```{python}
dateipfad = "01-daten/hooke_data.csv"
hooke = pd.read_csv(filepath_or_buffer = dateipfad, sep = ';')
hooke.drop(index = 113, inplace = True) # Fehlwert, siehe vorheriges Kapitel

hooke_503g = hooke.loc[hooke['mass'] == 503, :]
print(hooke_503g.head(), hooke_503g.describe(), sep = "\n\n")
```

Betrachten wir für diese Messreihe noch einmal das arithmetische Mittel (das unbereinigte Messergebnis) und den Standardfehler des Mittelwerts (das statistische Streuungsmaß des Mittelwerts). Die Methode `.sem()` führt die Berechnug des empirischen Standardfehlers des Mittelwerts aus (standardmäßig ist der Parameter `ddof = 1` gesetzt.)

Ausgabe auf 3 Dezimalstellen genau.

```{python}
print(f"Gewicht in Gramm: {hooke_503g['mass'].unique()}",
      f"Mittelwert der Messwerte: {hooke_503g['distance'].mean():.3f} cm",
      f"empirischer Standardfehler: {hooke_503g['distance'].sem():.3f} cm",
      sep = "\n")
```

:::: {.panel-tabset}
## keine systematische Messabweichung
Messunsicherheit auf 2 Dezimalstellen genau aufrunden. Dafür kann die NumPy-Funktion `np.ceil()` verwendet werden. Diese rundet allerdings zur nächsten Ganzzahl, ein Parameter, um auf eine bestimmte Dezimalstelle zu runden, ist nicht verfügbar. Deshalb muss die Dezimalstelle um die gewünschte Anzahl Stellen durch Multiplikation mit der entsprechenden Zehnerpotenz verschoben werden np.ceil(wert * 100) / 100`. Oder in Pandas:

```{python}
ceil_me = pd.Series([1.122])

print(ceil_me.mul(100).apply(np.ceil).div(100))
```

```{python}
print(f"Gewicht in Gramm: {hooke_503g['mass'].unique()}",
      f"Mittelwert der Messwerte: {hooke_503g['distance'].mean():.3f} cm",
      f"empirischer Standardfehler: { ( u_hooke_503g:= pd.Series(hooke_503g['distance'].sem()).mul(100).apply(np.ceil).div(100) ).item() } cm",
      f"vollständiges Messergebnis: {hooke_503g['distance'].mean().round(2)} cm ± {u_hooke_503g.item()} cm",
      sep = "\n")
```

Zusätzliche Angabe des 95-%-Konfidenzintervalls.

```{python}
# t-Wert bestimmen für Signifikanzniveau (alpha-Niveau) 1 - 95 % wählen
alpha = 0.05
n = hooke_503g.shape[0] # Anzahl Zeilen
t_wert = scipy.stats.t.ppf(q = 1 - alpha/2, df = n - 1)

print(f"95-%-Konfidenzintervall : {hooke_503g['distance'].mean().round(2)} cm  ± {t_wert:.4f} * {u_hooke_503g.item()} cm",
f"95-%-Konfidenzintervall : {hooke_503g['distance'].mean().round(2)} cm  ± {( t_wert * u_hooke_503g.item() ).round(2)} cm",
f"untere 95-%-Intervallgrenze: {hooke_503g['distance'].mean().round(2) - round(t_wert * u_hooke_503g.item(), 2)} cm",
f"obere 95-%-Intervallgrenze: {hooke_503g['distance'].mean().round(2) + round(t_wert * u_hooke_503g.item(), 2)} cm",
sep = "\n")


```

## mit systematischer Messabweichung
**to do: Ablesegenauigkeit + Messabweichung** 

::::


::: {#imp-wahrerwert .callout-important}
## Erwartungswert, wahrer Wert, unbekannte systematische Messabweichung

Wichtig: Die Annahme, dass das Konfidenzintervall für eine gewählte Wahrscheinlichkeit den wahren Wert $x_w$ einschließt, gilt nur für das um die bekannte systematische Messabweichung $e_{s,b}$ bereinigte Messergebnis $\bar x_E$. Für das unbereinigte Messergebnis $\bar x$ wird mit dem Konfidenzintervall der Erwartungswert $\mu$ geschätzt.

**Aber was ist mit der unbekannten systematischen Messabweichung?**

Die bekannte systematische Messabweichung entspricht nicht der gesamten systematischen Messabweichung. Die bekannte systematische Messabweichung wird als Schätzwert für die gesamte systematische Messabweichung verwendet, wobei eine Unsicherheit bezüglich des unbekannten Anteils der systematischen Messabweichung besteht. Diese Unsicherheit kann im Einzelfall vernachlässigbar sein. Dann gilt für den wahren Wert einer Größe $X$:

$$\bar x_E ~ — ~ t \cdot \frac{s}{\sqrt{n}} \le X \le \bar x_E + t \cdot \frac{s}{\sqrt{n}}$$

*Hinweis: In der Regel wird man die empirische Standardabweichung $s_{n-1}$ verwenden.*

Sollte die Unsicherheit der systematischen Messabweichung nicht als vernachlässigbar angesehen werden, verweist die DIN 1319-3 auf Berechnungsregeln in Abschnitt 6.2.5 - auf die wir hier nicht eingehen. (@DIN1319-3, S. 5-6)
:::

### 3. Fehlerfortpflanzung

**to do: erklären, dass es in der DIN 2 verschiedene Arten gibt, die Messunsicherheit anzugeben. -> einmal mit der Gaußschen Fehlerfortpflanzung, einmal via halbes Konfidenzintervall.**

Die Fehlerfortpflanzung wird verwendet, wenn

  - **mehrere Größen**,
  - **die durch eine Funktion die gesuchte Größe ergeben**,

gemessen werden.

Um die Messunsicherheit einer Größe $Y = f(x_1, x_2, ..., x_m)$ zu ermitteln, gibt es zwei Verfahren:

  - die **lineare Fehlerfortpflanzung** wird benutzt, wenn die Fehler der beteiligten Größen durch Fehlerabschätzung einer Einzelmessung ermittelt wurden.
  - die **Gauß'sche Fehlerfortpflanzung** wird benutzt, wenn die Einzelfehler statistisch ermittelt und mit Konfidenzintervallen angegeben werden.

[@Hempel-2016, S. 8-9]

#### Lineare Fehlerfortpflanzung
Die lineare Fehlerfortpflanzung betrachtet, wie sich die **Größtfehler** mehrerer gemessener Größen im ungünstigsten Fall auswirken. Es wird also der **Größtfehler** der gesuchten Größe $Y$ berechnet. (@DIN1319-3, S. 9)

Für eine Funktion mit zwei Eingangsgrößen $f = f(x_1, x_2)$ wird der Größtfehler $\Delta f$ aus den Größtfehlern $\Delta x_1$ und $\Delta x_2$ ermittelt.

$$
f + \Delta f = f(x_1 + \Delta x_1, x_2 + \Delta x_2)
$$

Unter der Voraussetzung gegenüber den Messwerten kleinen Fehler (**Wann sind die Fehler klein?**) kann der Fehler $\Delta f$ durch die Beträge der partiellen Ableitung des ersten Glieds der Taylorreihe berechnet werden (siehe Beispiel).

$$
f + \Delta f = f(x_1, x_2) + | \frac{\partial f(x_1, x_2)}{\partial x_1}  | \cdot \Delta x_1 + | \frac{\partial f(x_1, x_2)}{\partial x_2} | \cdot \Delta x_2
$$

::: {#nte-taylorreihe .callout-note collapse="true"}
## Taylorreihe

{{< video https://www.youtube.com/watch?v=F2lKRaUguBM >}}

Die Taylorreihe - einfach erklärt von Denkbar ist abrufbar auf [YouTube](https://www.youtube.com/watch?v=F2lKRaUguBM). 2015

:::

::: {#nte-partielleableitung .callout-note collapse="true"}
## Partielle Ableitung
Die partielle Ableitung ist die Ableitung einer Funktion mit mehreren Variablen nach einer Variablen, wobei die übrigen Variablen als Konstanten behandelt werden.

Für eine Funktion $f(x, y) = 2x + y^2$ wird die partielle Ableitung nach x so ausgedrückt:

$\frac{\partial f(x, y)}{\partial x}$

  - Das Symbol ∂ ist die kursive Darstellung des kyrillischen Kleinbuchstaben д (d) und wird als "d" oder "del" gelesen. Es zeigt an, das eine partielle Ableitung durchgeführt wird.
  - Im Zähler steht die Funktion, die abgeleitet werden soll. Im Nenner steht die Variable nach der abgeleitet wird. Der Term wird gelesen als "del f von x und y nach del x".

Die partielle Ableitung $\frac{\partial f(x, y)}{\partial x} = 2$.

Die partielle Ableitung $\frac{\partial f(x, y)}{\partial y} = 2y$.

:::

Der Größtfehler ergibt sich aus:
$$
\Delta f = | \frac{\partial f(x_1, x_2)}{\partial x_1}  | \cdot \Delta x_1 + | \frac{\partial f(x_1, x_2)}{\partial x_2} | \cdot \Delta x_2
$$

[@Hempel-2016, S. 9-10]

Bzw. in Kurzschreibweise für $i$ Eingangsgrößen:
$$
\Delta f(x_i) = \pm \sum_{i=1}^{N} | \frac{\partial f}{\partial x_i} \cdot \Delta x_i |
$$

[nach @EdenGebhard-2024, S. 35, Notation angepasst]

::: {.callout-tip}
## Größtfehler
"Für viele Betrachtungen, speziell in den Grundlagenpraktika der ersten Semester
in technischen, naturwissenschaftlichen und ingenieurmäßigen Studiengängen,
reicht es aus, den maximalen Fehler (*absoluter Größtfehler*) zu bestimmen." (@EdenGebhard-2024, S. 35)

**Warum eigentlich, der Fehler nach Gauß ist doch genauso leicht zu berechnen?!**
:::

#### Gauß’sche Fehlerfortpflanzung
Bei der Gauß’schen Fehlerfortpflanzung wird davon ausgegangen, dass sich die Messunsicherheit der beteiligten Größen kompensieren können. Die Gauß’sche Fehlerfortpflanzung ist deshalb für die Berechnung des Größtfehlers nicht geeignet. Vielmehr wird eine realistischere Abschätzung der Messunsicherheit erreicht.

Die Ermittlung der Messunsicherheit ist in DIN 1319-3 geregelt. Als Messunsicherheit der beteiligten Größen werden die Varianzen der Mittelwerte verwendet (DIN1319-3, Gleichung (29), S. 11).
$$
u^2(x_i) = s^2(\bar{x_i}) = s_i^2/n_i ~ ; ~ s_i^2 = \frac{1}{n_i - 1} \sum_{j = 1}^{n_i} (x_{ij - \bar{x_i}})^2
$$

*Hinweis: In der Regel wird man die empirische Standardabweichung $s_{n-1}$ verwenden.*  
*Hinweis: In der DIN wird der arithmetische Mittelwert einer Messreihe statt mit $\bar{x}$ mit $\bar{v}$ geschrieben*.

Die Messunsicherheit der beteiligten Größen wird also über den Standardfehler des Mittelwerts ermittelt:
$$
u(x_i) = s_i/\sqrt{n_i}
$$

*Hinweis: In der Regel wird man die empirische Standardabweichung $s_{n-1}$ verwenden.*

Wenn für eine Eingangsgröße nur ein einzelner Wert vorliegt, wird dieser als Schätzwert für $x_i$ verwendet. Die Unsicherheit ist aus den verfügbaren Informationen anzusetzen.

[@DIN1319-3, S. 11]

Die Unsicherheit der Funktion $y = f(x_1, x_2, ..., x_m)$ berechnet sich aus dem quadrierten Produkt der partiellen Ableitungen und der individuellen Messunsicherheiten, aus deren Summe die Quadratwurzel gezogen wird (DIN 1319-3, Gleichung (43)):

$$
u(y) = \sqrt{\sum_{i = 1}^{m} (\frac{\partial f}{\partial x_i})^2 \cdot u^2(x_i)}
$$

Die quadrierten Terme können auch zusammengefasst werden:

$$
u(y) = \sqrt{\sum_{i = 1}^{m} \left(\frac{\partial f}{\partial x_i} \cdot u(x_i)\right)^2}
$$

Für eine Funktion mit zwei Eingangsgrößen $y = f(x_1, x_2)$ berechnet sich die Unsicherheit beispielsweise wie folgt:

$$
u(y) = \sqrt{ (\frac{\partial f}{\partial x_1} \cdot \bar{s}_{x_1})^2 + (\frac{\partial f}{\partial x_2} \cdot \bar{s}_{x_2})^2 }
$$

*Hinweis: In der Regel wird man die empirische Standardabweichung $s_{n-1}$ verwenden.* 

#### Spezialfall: Fehlerfortpflanzung der Federkonstante
**dann Fehler bei linearere Regression (Federkonstante). Dafür die DIN1319-4 lesen. Die ist schwer zu lesen, aber am Ende nimmt man den Standardfehler des Regessionskoeffizienten.**
Schätzwerte der Eingangsgrößen sind wie eben die jeweiligen Standardfehler der Mittelwerte (@DIN1319-4, Gleichung 25, S. 10).
Gemeinsame Komponenten der Messunsicherheit - also die Kovarianz und Korrelation der Eingangsgrößen - darf gleich Null gesetzt werden, wenn die Größen unkorreliert sind. (@DIN1319-4, S. 12). Die gemeinsame Komponente der Messunsicherheit ist in der DIN als $u(x_k, x_l)$ notiert (ist sehr unleserlich).
-> Soweit ist es das Gleiche wie in deiner normalen Modellfunktion. **Wann kommt den der Standardfehler des Korrelationskoeffizienten ins Spiel? Gar nicht! -> Das ist die erweiterte Messunsicherheit U(y) und die DIN 1319-3 formuliert es so:**
"Ein **anderer** Kennwert für die Genauigkeit einer Messung ist die erweiterte Meßunsicherheit". [@DIN1319-3, S. 7]


  - "Für eine ausgewählte Ergebnisgröße kann zusätzlich die erweiterte Meßunsicherheit und ein (angenähertes) Vertrauensintervall zu einem zu nennenden Vertrauensniveau mitgeteilt werden (siehe 10.1). Bei der Methode der kleinsten Quadrate (siehe Abschnitt 8) sind schließlich der Wert der benutzten Prüfgröße $\xi_k$ [xi k] und die globale Standardmeßunsicherheit $u$ von Interesse." (@DIN1319-4, S. 18)
    - $\xi_k$ ist die Summe der quadrierten Abstände der Vorhersagewerte $\hat{x_k}$ von den tatsächlich gemessenen Werten $x_k$? [@DIN1319-4, Gleichung 55, S. 15] 
    - Was ist $u$? -> das kann doch nur die Standardmeßunsicherheit u(y) sein.

  - 10.1 Vertrauensbereich, Gleichung 76, S. 19: "Für eine einzelne ausgewählte Ergebnisgröße $Y$ kann jedoch näherungsweise ein Vertrauensintervall* (Konfidenzintervall*) zum Vertrauensniveau $1 — \alpha$ angegeben werden, das eingeschlossen wird von den Vertrauensgrenzen* $y — ku(y)$ und $y + ku(y)$. Hier ist $U(y) = ku(y)$ die erweiterte Meßunsicherheit [...]."
  "Für $1 — \alpha = 68,27 \%$ ist $k = 1$ und die Standardmeßunsicherheit $u(y)$ gleich der halben Weite des Vertrauensintervalls. (siehe auch Gleichung (75)). Für $1 — \alpha \approx 95 \%$ ist $k = 2$. Dieser Wert wird im industriellen Meßwesen bevorzugt, $k$ ist auch das $(1 — \alpha/2)$-Quantil* der standardisierten Normalverteilung* [...]." (@DIN1319-4, S. 19)

Die erweiterte Messunsicherheit ist also als das Vertrauensintervall definiert.

Der Erweiterungsfaktor k ist anzugeben. Nur so lässt sich die Standardunsicherheit $u(y) = U(y) / k$ wieder berechnen. (@DIN1319-3, S. 7)

Also man gibt ein Vertrauensintervall für ein gewähltes Vertrauensniveau.

  Gleichung 70: $u^2 = (z-x)^T (z-x) / \nu$    
Bei unkorrelierten Eingangsgrößen und einer Ergebnisgröße Y **Den Begriff oben ebenfalls verwenden** lautet die Gleichung:

$$
u(y) = \sqrt{\sum_{k=1}^{m} (\frac{\partial G}{\partial x_k})^2 \cdot u^2(x_k)}
$$

  - G ist die Modellfunktion
  - m ist die Anzahl Eingangsgrößen

Und ein "Fundstück": "ANMERKUNG 2: Gleichung (39) wurde früher "Fehlerfortpflanzungsgesetz" genannt. Sie betrifft jedoch nicht die Fortpflanzung von Meßabweichungen (früher 'Fehler'), sondern die von Unsicherheiten." [@DIN1319-4, Gleichung 39, S. 13]

[@DIN1319-4, Gleichung 39, S. 13]

Problematisch ist allerdings die Ermittlung der gesamten Messunsicherheit, wenn diese sich aus statistischen Vertrauensbereichen und abgeschätzten Fehleranteilen aus Einzelmessungen zusammensetzt. Beispielsweise hängt die im vorherigen Kapitel ermittelte Federkonstante von drei Größen ab:

  - der gemessenen Masse **Die Messabweichung der Waage muss noch eingeführt werden.**,
  - dem mit einem Abstandssensor gemessenen Abstand zum Gewicht bzw. nach der Aufbereitung der Daten der Ausdehnung der Feder und
  - der statistischen Schätzung der Federkonstante.

Für die Berechnung eines Gesamtfehlers, muss ein Kompromiss eingegangen werden.

1. Die lineare Fehlerfortpflanzung wird mit dem abgeschätzten Fehler und dem statistischen Vertrauensbereich durchgeführt.
2. Die Gauß’sche Fehlerfortpflanzung wird mit dem statistischen Vertrauensbereich durchgeführt und ein Fehleranteil addiert, der aus der linearen Fehlerfortpflanzung der abgeschätzten Fehler ermittelt wird.

"Beide Verfahren sind wohl gleich gut (oder schlecht) als Kompromiss in der Laborpraxis geeignet." (@Hempel-2016, S. 9)

(@Hempel-2016, S. 8 - 9)

Wenn für eine Eingangsgröße nur ein einzelner Wert vorliegt, wird dieser als Schätzwert für $x_i$ verwendet. Die Unsicherheit ist aus den verfügbaren Informationen oder nach der Erfahrung anzusetzen. [@DIN1319-3, S. 11]


::: {.callout-important}
## Formelbahnhof


--> das ist die lineare Fehlerfortpflanzung

"so beträgt die Standardabweichung $\epsilon y$ (der absolute Fehler) des Messergebnisses y:

$$
\epsilon y = \pm \sqrt {\sum_{i=1}^{N} (\frac{\partial F}{\partial x_i} \epsilon x_i)^2}
$$

"

--> das ist Gauß'sche Fehlerfortpflanzung

(@EdenGebhard-2014, S. 34 **to do: aktualisieren**)

:::

#### Spezialfall: Fehlerfortpflanzung der Federkonstante
**Problem:**

  - der Abstand wurde mehrmals gemessen. Das ist ein statistischer Fehler - der Standardfehler (der vermutlich mit einem Vertrauensbereich 1t, 2t oder $t_{95\%}$ multipliziert werden muss). Der größte Standardfehler liegt für 301 Gramm mit 0.525247 vor. ... und der muss mit der Gauß'schen Fehlerfortpflanzung berechnet werden.
  - Da aber der Größtfehler gesucht ist, müsste eigentlich die lineare Fehlerfortpflanzung verwendet werden, bei der sich die einzelnen Fehler nicht gegenseitig aufheben können. Aber wie ist das mit mehreren Messreihen für verschiedene Gewichte? Für die Masse haben wir ja keinen gemeinsamen Standardfehler.

Die im vorherigen Kapitel ermittelte Federkonstante hängt von drei Größen ab:

  - der gemessenen Masse,
  - dem mit einem Abstandssensor gemessene Abstand zum Gewicht bzw. nach der Aufbereitung der Daten der Ausdehnung der Feder und
  - der statistischen Schätzung der Federkonstante.

Um den Gesamtfehler mehrerer fehlerbehafteter Größen zu ermitteln, müssen die Fehler der beteiligten Größen bekannt sein oder ermittelt werden. 

  - Die Masse $m$ der verwendeten Gewichte wurde mit einer Küchenwaage ermittelt, deren absoluter Fehler auf $\epsilon m = 0,5 g = 0,0005 kg$ geschätzt wird.
  - Der Abstand $s$ wurde mit einem elektronischen Abstandssensor und einem Arduino ermittelt (Ultrasonic Ranging Module HC - SR04).
     - Der Ablesefehler des Messsystems beträgt eine Einheit der Messauflösung, also $\epsilon s_{zufällig} = 0,01 cm = 0,0001 m$.
     - Die Messabweichung des Messsystems beträgt laut Datenblatt bis zu 3 mm, also $\epsilon s_{systematisch} = 3 mm = 0,003 m$.
     - Der abgeschätzte Fehler beträgt also $\epsilon x = 0,0001 m + 0,003 m = 0,0031m$
  - Der Standardfehler der Federkonstante $k$ wurde mit $0.3784 \frac{N}{m}$ bestimmt. Im festgelegten Vertrauensintervall $95 \%$ beträgt der Fehler der Federkonstante $\epsilon k = 0.75 \frac{N}{m}$

Für die Federkonstante gilt (die Abstandsänderung $\Delta x$ wird mit $s$ notiert):

$$
k = \frac{m \cdot g}{\Delta{x}} = \frac{m \cdot g}{s}
$$

So gilt für den Größtfehler der Funktion:

$$
\Delta k(m, s) = | \frac{\partial f(m, s)}{\partial m} | \cdot \Delta m + | \frac{\partial f(m, s)}{\partial s} | \cdot \Delta s
$$

Die partielle Ableitung nach m lautet mit Faktorregel:

$$
\frac{\partial f(m, s)}{\partial m} = \frac{\partial}{\partial m} \frac{m \cdot g}{s} = \frac{\partial}{\partial m} m \cdot \frac{g}{s} = 1 \cdot \frac{g}{s} = \frac{g}{s}
$$

Die partielle Ableitung nach s lautet:

$$
\frac{\partial f(m, s)}{\partial s} = \frac{\partial}{\partial s} \frac{m \cdot g}{s} = - \frac {m \cdot g}{s^{2}}
$$

Werden die Beträge eingesetzt und mit dem jeweiligen Größtfehler multipliziert ergibt sich aus der linearen Fehlerfortpflanzung der Größtfehler von $k$ durch:

$$
\Delta k = | \frac{g}{s} | \cdot \Delta m ~ + ~ | (-) \frac {m \cdot g}{s^{2}} | \cdot \Delta s
$$

Für die mit verschiedenen Gewichten durchgeführten Messungen müssen nun sinnvolle Werte gefunden werden, die für die Masse $m$ und die Abstandsänderung $s$ verwendet werden können. Einerseits soll das Prinzip der Größtfehlerbestimmung, den ungünstigsten Fall widerzuspiegeln, gewahrt werden. Andererseits soll der sinkenden Messunsicherheit infolge der durch die Messung mit mehreren Gewichten steigenden Stichprobengröße Rechnung getragen werden. 

Deshalb soll ein gewichteter Größtfehler bestimmt werden, der große Fehler stärker berücksichtigt. Dazu wird die lineare Fehlerfortpflanzung für jedes der verwendeten Gewichte durchgeführt. Die so gewonnenen Größtfehler werden quadriert, gemittelt und anschließend durch die Quadratwurzel zu einem gewichteten Größtfehler zusammengefasst.  
(*Hinweis: Es gibt meiner Kenntnis nach kein etabliertes Verfahren dazu und es handelt sich hierbei um einen methodischen Vorschlag.* **Das ist Quatsch - musst du DIN 1319-4 lesen.)

**Größtfehler der Masse**
Soll für jedes Gewicht mit dem statistischen Vertrauensbereich der Strecke durchgeführt werden. Da die Strecke im Nenner steht, nimmt man die untere Grenze des Vertrauensintervalls **ist ein Vorschlag von mir**. 

$\bar{s} - t_{1-\alpha / 2} ~ (n - 1) \frac{s}{\sqrt{N}}$

``` {python}
# Messdaten aus dem Kapitel hooke.qmd wiederherstellen
## Datei einlesen und Fehler entfernen
dateipfad = "01-daten/hooke_data.csv"
hooke = pd.read_csv(filepath_or_buffer = dateipfad, sep = ';')
hooke.drop(index = 113, inplace = True)

## Abstandsmessung auf Nullpunkt normieren
nullpunkt = hooke[hooke['mass'] == 0].loc[: , 'distance'].mean()
hooke['distance'] = hooke['distance'].sub(nullpunkt).mul(-1)

## Masse in kg, Abstand in m umrechnen
hooke['mass'] = hooke['mass'].div(1000)
hooke['distance'] = hooke['distance'].div(100)

## untere Intervallgrenze bestimmen
### mittlere Ausdehnung nach Gewicht berechnen (in m)
distance_means_by_weight = hooke['distance'].groupby(by = hooke['mass']).mean()
distance_means_by_weight.name = 'Federausdehnung in m'

### Standardfehler nach Gewicht berechnen (in m)
distance_stderrors_by_weight = hooke['distance'].groupby(by = hooke['mass']).std(ddof = 1).div(np.sqrt(hooke['distance'].groupby(by = hooke['mass']).size()))
distance_stderrors_by_weight.name = 'Standardfehler in m'

### n bestimmen
sizes_by_weight = hooke['distance'].groupby(by = hooke['mass']).size()
sizes_by_weight.name = 'Stichprobengröße'

### Signifikanzniveau (alpha-Niveau) 1 - 95 % wählen
alpha = 0.05

### unter Inervallgrenzen ausrechnen
untere_intervallgrenze_abstand = scipy.stats.t.ppf(q = alpha / 2, loc = distance_means_by_weight, scale = distance_stderrors_by_weight, df = sizes_by_weight - 1)

# Ausgabe
df_abstand = pd.DataFrame({'mittlere Abstandsänderung': distance_means_by_weight,
                   'Standardfehler': distance_stderrors_by_weight,
                   'untere 95-%-Intervallgrenze': untere_intervallgrenze_abstand})

## wissenschaftliche Notation in der Ausgabe unterdrücken
print(df_abstand.round(6))
```

**An der Schreibweise muss man noch arbeiten... N ist ja eigentlich die Stichprobengröße, mit i haben wir die einzelnen Messwerte bezeichnet - gemeint ist die Anzahl der Messreihen.**
$$
\sqrt{\frac {1}{N} \sum_{i=1}^{N} (\frac{g}{{s_{\alpha / 2}}_i} \cdot \Delta m_i)^2}
$$

**gewichteten Größtfehler der Masse berechnen**

Wurzel des Mittelwerts der Größtfehlerquadrate

``` {python}
fehler_masse_kg = 0.0005

gewichteter_größtfehler_masse = pd.Series((9.81 / untere_intervallgrenze_abstand * fehler_masse_kg)).pow(2).mean() ** 1/2

print("Gewichteter Größtfehler der Masse:", gewichteter_größtfehler_masse)
```


**Größtfehler der Strecke**
Für den Größtfehler der Strecke wird ebenfalls die untere Intervallgrenze verwendet, da die Strecke im Nenner steht. Das negative Vorzeichen entfällt aufgrund der Betragsbildung.

**An der Schreibweise muss man noch arbeiten... N ist ja eigentlich die Stichprobengröße, mit i haben wir die einzelnen Messwerte bezeichnet - gemeint ist die Anzahl der Messreihen.**
$$
\sqrt{\frac {1}{N} \sum_{i=1}^{N} (\frac{m \cdot g}{{s^2_{\alpha / 2}}_i} \cdot \Delta m_i)^2}
$$

**gewichteten Größtfehler der Strecke berechnen**  
*Für die Masse nehmen wir keine Intervallgrenze, weil die Masse einmal gemessen wurde und dieser Fehler im Größtfehler steckt*

``` {python}
fehler_strecke_m = 0.0031

## die Masse kann in der korrekten Reihenfolge aus dem Index abgegriffen werden. hooke['mass'].unique() gibt die Massen absteigend zurück

gewichteter_größtfehler_strecke = pd.Series(9.81 * distance_means_by_weight.index / untere_intervallgrenze_abstand ** 2 * fehler_strecke_m).pow(2).mean() ** 1/2

# Ausgabe
print("Gewichteter Größtfehler Strecke:", gewichteter_größtfehler_strecke)
print()

## Ausgabe der Zwischenergebnisse
größtfehler_strecke_nach_messreihe = pd.Series(9.81 * distance_means_by_weight.index / untere_intervallgrenze_abstand ** 2 * fehler_strecke_m)

df_strecke = pd.DataFrame({'Gewicht': distance_means_by_weight.index, 'Größtfehler der Strecke der Messreihen': größtfehler_strecke_nach_messreihe, 'quadratischer Größtfehler': größtfehler_strecke_nach_messreihe ** 2})

print(df_strecke)
print('mittlerer Größtfehler', df_strecke['Größtfehler der Strecke der Messreihen'].mean())

```

$$
\Delta k = 0.183 + 3.468 = 3.651
$$


Für die Masse müsste noch ein Größtfehler geschätzt werden... das ist bislang nur die Ableseungenauigkeit

  - Die Masse $m$ der verwendeten Gewichte wurde mit einer Küchenwaage ermittelt, deren absoluter Fehler auf $\epsilon m = 0,5 g = 0,0005 kg$ geschätzt wird **Das ist doch nur der Ablesefehler oder (sieht nach halbem Skalenstrich aus?!**.
  - Der Abstand $s$ wurde mit einem elektronischen Abstandssensor und einem Arduino ermittelt Der absolute Fehler dess Messsystems beträgt eine Einheit der Messauflösung, also $\epsilon s = 0,01 cm$, die Garantiefehlergrenze $3 mm$. Der Größtfehler des Abstands beträgt also $\Delta s = 0,0001 + 0,003 m = 0,0031 m$.
  - Der Standardfehler der Federkonstante $k$ wurde mit $0.3784 \frac{N}{m}$ bestimmt.


## Resterampe

Größtfehler bei Messung mehrerer fehlerbehafteter Größen: Fehlerfortpflanzung  
https://www.uni-ulm.de/fileadmin/website_uni_ulm/nawi.inst.225/teaching/Grundpraktikum/Fehlerrechnung/Einfuehrung_FehlerrechnungHankelPaul.pdf

Wiederholung Fehlerstatistik Steigungskoeffizient der linearen Regression

1. Legen Sie ein Signifikanzniveau $\alpha$ bzw. ein zweiseitiges Konfidenzniveau $1 - \frac{\alpha}{2}$ fest.
2. Bestimmen Sie den Steigungskoeffizienten $\hat{\beta_1}$
3. Bestimmen Sie den Standardfehler des Steigungskoeffizienten $SE = \hat{\sigma}_{\hat{\beta_1}}$
4. Bestimmen Sie den Wert der t-Verteilung mit n - 2 Freiheitsgraden für das gewählte Konfidenzniveau und einen zweiseitigen Hypothesentest $t_{1-\alpha / 2} ~ (n - 2)$
5. Berechnen Sie das Konfidenzintervall $\hat{\beta_1} \pm SE \cdot t_{1-\alpha / 2} ~ (n - 2)$

Der Größtfehler $\Delta x$ setzt sich zusammen aus dem systematischen Fehler und dem zufälligen Fehler. 

- systematische Fehler: Fehler der Messgeräte, der Art der Messung (bspw. Genauigkeit Abstandssenor) --> sollten, wenn sie bekannt sind, korrigiert werden!
  - das ist die Küchenwaage, deren systematischer Fehler auf  $\frac{g}{\Delta x} = \frac{9.81}{\Delta x}$ ? Was ist Delta-x, die Ausdehnung?!
    - Die Masse wäre 705g --> müsste die Messungenauigkeit der Küchenwaage mit 0.5 g nicht ins Verhältnis zu 705 g gesetzt werden?
- zufällige Fehler: Streuung von Messwerten um Erwartungswert --> sollten quantifiziert werden.




Bei der Fehlerabschätzung von 
Addition aus Fehlerabschätzung und Fehlerstatistik, beispielsweise t-Wert des gewählten Konfidenzniveaus mal Standardfehler einer Messreihe plus minus Fehlerabschätzung

"
- Bei Einfachmessung geht der Garantiefehler des Messgerätes ein, der Ablesefehler des Experimentators,
Fehler, die aus Umwelteinflüssen resultieren und gegebenenfalls noch vieles andere.
Alle diese Fehler werden so gut es eben geht abgeschätzt, addiert und bilden in ihrer Gesamtheit
die sogenannte Messunsicherheit.




Der gute Herr Hempel schreibt also, 2t würde man üblicherweise nehmen.

[Hier (S. 9)](https://users.physics.unc.edu/~deardorf/uncertainty/UNCguide.pdf) steht man würde die Standardunsicherheit überweise im Intervall von ca. 68 % - das ist eben 1t bzw. 1s - angeben. Bei vielfachen von s ist es eigentlich auch egal was man nimmt.

[Hier (S. 20)](https://users.physics.unc.edu/~deardorf/uncertainty/UNCguide.pdf) wird ein Trick - das gemeinsame Messen - zur Redzuierung des systematischen Messfehlers genannt (wenn anwendbar.)

"Die Benennung Standardmeßunsicherheit, kurz Standardunsicherheit [1], wird verwendet, wenn herausgestellt werden soll, daß die Meßunsicherheit durch eine Standardabweichung Standardabweichung* ausgedrückt wird [...]." [@DIN1319-3, S. 3]

"Die Standardabweichung
ist ein Maß für die Streuung der einzelnen Meßwerte um den Erwartungswert oder der zufälligen Meßabweichungen*
um Null." [@DIN1319-3, S. 5]

erweiterte Messunsicherheit vs. Vertrauensbereich 
DIN 1319-3 definiert einerseits die erweiterte Messunsicherheit als U(y) = k * u(y). Der Erweiterungsfaktor k soll zwischen 2 und 3 liegen und vorzugsweise = 2 betragen. (DIN 1319-3, S. 7)
Andererseits wird der Vertrauensbereich, als optionale, zusätzliche Angabe zum vollständigen Messergebnis (bspw. Y = y plus / minus u(y)) unter Verweis auf DIN 1319-1 definiert: u* = t * s / sqrt(n) (DIN 1319-1, S. 14, DIN 1319-3, S. 7). 
